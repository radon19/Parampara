BEE_URL=http://localhost:1633
PAYER_ADDRESS=
PUBLISHER_PRIVATE_KEY=
GOVERNANCE_PRIVATE_KEY=
# BATCH_ID=
# INCOMING_STEWARD=
# Optional Safe
# SAFE_ADDRESS=
# Committee allowlist for /succeed proposers (server-only, comma-separated).
# When empty, the deny-list (current + past stewards, payer) applies.
# COMMITTEE_ADDRESSES=
# SAFE_TX_SERVICE_URL=https://safe-transaction-gnosis-chain.safe.global
# WalletConnect cloud id (not a private key, keep in env)
# NEXT_PUBLIC_WALLETCONNECT_ID=
# Node wallet shown in UI as payer (must differ from publisher)
# NEXT_PUBLIC_PAYER_ADDRESS=
